# ==============================================================================
# SafeVision AI — Backend Dependencies
# Stack: FastAPI + SQLAlchemy + Alembic + PostgreSQL + Redis/Celery
# ==============================================================================

# --- Web Framework ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9      # For form data / file uploads

# --- Database (PostgreSQL + SQLAlchemy + Alembic) ---
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0   # PostgreSQL driver
pgvector>=0.3.0,<1.0.0           # pgvector extension support (Phase 21, enabled now)

# --- Auth & Security ---
passlib[bcrypt]>=1.7.4,<2.0.0   # Password hashing (bcrypt)
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT encoding/decoding
bcrypt>=4.0.0,<5.0.0

# --- Background Jobs (Redis + Celery) ---
celery>=5.4.0,<6.0.0
redis>=5.0.0,<6.0.0

# --- Validation & Serialization ---
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
email-validator>=2.0.0

# --- HTTP Client (for notification providers, LLM calls) ---
httpx>=0.27.0,<1.0.0

# --- Notification Providers (Phase 15) ---
twilio>=9.0.0,<10.0.0   # WhatsApp via Twilio REST API

# --- CORS / Rate Limiting ---
slowapi>=0.1.9,<1.0.0           # Rate limiting for FastAPI

# --- Logging ---
structlog>=24.0.0,<25.0.0       # Structured JSON logging

# --- Document / RAG Processing ---
pypdf>=4.0.0,<6.0.0

# --- Dev / Test ---
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<6.0.0
httpx                            # Also used as async test client for FastAPI
ruff>=0.5.0                      # Linter + formatter

